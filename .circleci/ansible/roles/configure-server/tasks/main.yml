---
  - name: "update apt packages."
    become: yes
    apt:
      update_cache: yes

  - name: "upgrade packages"
    become: yes
    apt:
      upgrade: yes

  - name: "install nodejs and npm"
    become: yes
    apt:
      name: ["nodejs", "npm", "git"]
      state: latest
      update_cache: yes
